## 二分探索
- `A = [F, F, T, T, T]`: 初めて`T`となる場所を返す
```cpp
template <typename F>
ll my_lower_bound(ll L, ll R, F check) {
  while (L < R) {
    ll mid = L + (R - L) / 2;
    if (check(mid)) {
      R = mid;
    } else {
      L = mid + 1;
    }
  }
  return L;
}
```