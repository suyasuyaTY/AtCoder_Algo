## Zobrish hashing
- 集合をハッシュにすることで、集合の一致判定が $O(1)$ でできる
- 集合 $A$ のハッシュから、$A$ に1要素追加/削除した時のハッシュを $O(1)$で計算できる
- 集合 $A$ のハッシュと集合 $B$ のハッシュから、xorのハッシュを$O(1)$で計算できる

## やり方
1. 集合の要素として出てくる値にランダムな整数を割り当てる。
1. 集合のハッシュは、その集合の各要素に割り当てられた整数の総 XOR となる。